# Origem da API, sem `/v1`: o caminho da rota vem do contrato. `localhost` não funciona
# no emulador — ele resolve para o próprio aparelho, não para a máquina que roda a API.
# Emulador Android: http://10.0.2.2:3000 · Aparelho físico: http://<IP-LAN>:3000
EXPO_PUBLIC_API_ORIGIN=http://10.0.2.2:3000
