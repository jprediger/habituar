{
  "extends": "../tsconfig/base.json",
  "_comment": "Sem tipos ambientes: a prova é que a base não carrega a lib DOM, e um @types hoisteado (jsdom) a traria de volta — a fixture passaria por engano.",
  "compilerOptions": { "noEmit": true, "types": [] },
  "include": ["forbidden/dom-in-core.ts"]
}
