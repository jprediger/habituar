{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Sem DOM: primitivo de web dentro do app nativo é erro de compilação, não de runtime.",
  "extends": "./base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "noEmit": true
  }
}
