{
  "extends": "@habituar/config/tsconfig/base.json",
  "_comment": "Resolução de bundler porque o fonte nunca é consumido cru: o tsup empacota o entrypoint antes de qualquer app enxergar o pacote. Sem lib DOM: o pacote é headless (D2), então `jsx: react-jsx` basta para compilar JSX sem herdar tipos de navegador.",
  "compilerOptions": {
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src", "tsup.config.ts"]
}
