/** Tamanho é número (sem unidade); line-height é razão, nunca px. */
export const FONT_SIZE = {
  caption: 12,
  body: 16,
  title: 20,
  display: 28,
} as const

export const FONT_WEIGHT = {
  regular: 400,
  medium: 500,
  bold: 700,
} as const

export const LINE_HEIGHT = {
  tight: 1.25,
  normal: 1.5,
} as const
