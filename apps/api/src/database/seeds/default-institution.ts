/**
 * Nome da instituição criada pelo seed de admin e procurada pelo seed de
 * desenvolvimento. Existe como constante compartilhada porque os dois seeds precisam
 * concordar sobre ele; divergir significa o seed de desenvolvimento não achar nada.
 */
export const DEFAULT_INSTITUTION_NAME = 'Instituição Padrão'
